# Fonts

Place your `.ttf`, `.otf`, `.woff`, or `.woff2` font files here.

They will be automatically copied to each platform's resources during build:
- **Web**: `webApp/src/wasmJsMain/resources/fonts/` (via Gradle task)
- **Android**: referenced from this folder
- **iOS**: referenced from this folder
